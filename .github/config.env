# Configuration pour GitHub Actions - LOLDrivers Updater

# URL source des données LOLDrivers
REMOTE_URL=https://raw.githubusercontent.com/magicsword-io/LOLDrivers/refs/heads/main/loldrivers.io/content/api/drivers.json

# Fichier local à mettre à jour
LOCAL_FILE=data/drv.json

# Planification (format cron)
# Actuellement : Chaque mardi à 6h UTC
# Format : minute heure jour_du_mois mois jour_de_la_semaine
CRON_SCHEDULE="0 6 * * 2"

# Options de configuration
ENABLE_DETAILED_DIFF=true          # Afficher les différences détaillées
ENABLE_STATISTICS=true             # Calculer et afficher les statistiques
ENABLE_CLEANUP=true                # Nettoyer les fichiers temporaires
AUTO_COMMIT=true                   # Créer automatiquement un commit

# Messages de commit personnalisés
COMMIT_MSG_NEW="🆕 Add initial LOLDrivers data file"
COMMIT_MSG_UPDATE="🔄 Update LOLDrivers data"

# Timezone pour les logs
TIMEZONE="UTC"

# Branches autorisées pour les mises à jour
ALLOWED_BRANCHES="main,master"

# Seuil minimum de changement (en bytes) pour déclencher une mise à jour
MIN_CHANGE_THRESHOLD=100
